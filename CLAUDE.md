# 项目：我的博客

一个纯前端的中文个人博客网站，无构建工具，无框架依赖。

## 技术栈

- 纯 HTML / CSS / JavaScript（原生，无框架）
- CSS Variables 实现主题切换（亮色/暗色）
- 响应式设计（支持桌面、平板、手机）

## 文件结构

- `index.html` — 主页面，包含导航栏、Hero 区、文章卡片列表、侧边栏、页脚
- `style.css` — 全部样式，含 CSS 变量、暗色主题、响应式断点（1024/768/480px）
- `script.js` — 交互逻辑：主题切换、搜索、移动端菜单、标签筛选、分页、订阅表单、滚动效果

## 功能模块

- **主题切换**：暗色/亮色模式，使用 `localStorage` 持久化
- **标签筛选**：全部 / 技术 / 生活 / 设计 / 人工智能 / 旅行
- **搜索**：按文章标题和摘要实时过滤
- **移动端菜单**：汉堡菜单动画，响应式导航
- **订阅表单**：模拟异步提交
- **滚动动画**：IntersectionObserver 驱动侧边栏卡片入场

## 设计规范

- 主色调：`#6366f1`（Indigo）
- 字体：系统字体栈（PingFang SC / Microsoft YaHei）
- 圆角：8px / 12px / 16px / 24px
- 最大宽度：1160px

## 开发注意事项

- 没有构建步骤，直接编辑文件即可
- 图片使用 Unsplash 外链，带 lazy loading
- 文章数据是硬编码在 HTML 中的（非动态加载）
